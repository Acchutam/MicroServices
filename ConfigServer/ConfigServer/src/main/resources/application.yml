server:
  port: 8085
  
spring:
  application:
    name: CONFIG-SERVER
   
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Acchutam/microservice-config.git
          clone-on-start: true 